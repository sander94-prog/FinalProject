<h1>Updating Lots</h1>

<%= form_for :parking, url: parking_path(@parking), method: :patch do |f| %>
 
  <% if @parking.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@parking.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @parking.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>
 
  <p>
    <%= f.label :address %><br>
    <%= f.text_area :address %>
  </p>
  
  <p>
    <%= f.label :spots %><br>
    <%= f.text_field :spots %>
  </p>
  
   <p>
    <%= f.submit %>
  </p>
 
<% end %>
<%= link_to 'Back', parkings_path %>