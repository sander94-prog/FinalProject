<h1>New Parking Lot</h1>
<%= form_for :bojangle, url: bojangles_path do |f| %>

<% if @bojangle.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@bojangle.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @bojangle.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<p>
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </p>
 
  <p>
    <%= f.label :address %><br>
    <%= f.text_field :address %>
  </p>
  
  <p>
    <%= f.label :spots %><br>
    <%= f.text_field :spots %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
 
<% end %>
<%= link_to 'Back', bojangles_path %>
