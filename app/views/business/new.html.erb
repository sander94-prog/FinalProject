<h1>Business details</h1>
<%= form_for :business, url: business_path do |f| %>

<% if @business.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@business.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @business.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

 <p>
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </p>
 
  <p>
    <%= f.label :address %><br>
    <%= f.text_area :address %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
 
<% end %>
<%= link_to 'Back', business_path %>
