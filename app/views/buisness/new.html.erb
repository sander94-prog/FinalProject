<h1>New Parking Lot</h1>
<%= form_for :buisness, url: buisness_path do |f| %>

<% if @buisness.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@buisness.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @buisness.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

 <p>
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </p>
 
  <p>
    <%= f.label :address %><br>
    <%= f.text_area :address %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
 
<% end %>
<%= link_to 'Back', buisness_path %>
